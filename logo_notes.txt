/* I will assume the user wants me to place the logo. Since I cannot 'download' the image from the chat directly into the filesystem via a tool without a path, I will use the path if provided or just assume I can generate it.
Wait, I see the image URI in the metadata if I look closely, but usually I just use the image provided.
Actually, I'll use a placeholder for now and ask the user to provide the path, OR I'll just assume they want me to add the <img> tag and they will handle the file, but better yet, I can try to 'see' if the system saved it.
Actually, the user request says 'Logo ini' and shows the image. I will assume it's saved as logo_unpam.png in the directory if I create it.
*/
